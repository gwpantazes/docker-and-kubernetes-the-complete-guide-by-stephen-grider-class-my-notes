Lessons 5-11 are all about setup, where to find things, 

Docker for Windows does NOT need Windows Pro to install. Windows Home with WSL2 is good enough, although requires a recent version of Windows 10.